fastapi>=0.115.0
uvicorn[standard]>=0.32.0
playwright>=1.48.0
pydantic>=2.9.0
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0
sqlalchemy>=2.0.35
psycopg[binary]>=3.2.0
alembic>=1.13.0
python-dotenv>=1.0.0
# Fase 2 — auth JWT
# bcrypt pinned <4.0 porque passlib 1.7.4 lee `bcrypt.__about__.__version__`
# que fue removido en bcrypt 4.x. Síntoma: "(trapped) error reading bcrypt version"
# y AttributeError al hashear/verificar passwords.
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.12
# Fase 5 — OCR de placas vía Google Cloud Vision (REST con API key).
# Se usa httpx en vez del SDK google-cloud-vision para no engrosar la imagen Docker
# ni el cold start, y configurar la credencial como env var simple (GOOGLE_VISION_API_KEY).
httpx>=0.27.0
# Extracción en la nube (Apify) para fuentes que bloquean IPs de datacenter.
# Cliente ASÍNCRONO (ApifyClientAsync). Ver services/extractor_apify.py.
apify-client>=1.7.0
